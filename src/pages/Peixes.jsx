import { Ferramentas } from "components";
import { Base } from "layout";

export const Peixes = () => {
    return (
        <Base
            titulo="Relatório dos Peixes"
            barra={<Ferramentas />}>
            Conteúdo
        </Base>
    );
};