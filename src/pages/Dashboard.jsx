import { Base } from "layout";

export const Dashboard = () => {
    return (
        <Base
            titulo="Dashboard"
            barra={undefined}>
            Conteúdo
        </Base>
    );
};