import { Box, Typography } from "@mui/material";

export const Dashboard = () => {
    return (
        <Box>
            <Typography variant="h4">
                Dashboard
            </Typography>
        </Box>
    );
};